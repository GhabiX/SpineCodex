// Replay event adaptation lives in `parser.rs`.
//
// Keep this file as an empty tombstone for now because project policy avoids
// deleting files during active multi-agent refactors. Reclaim it in the final
// parser module layout cleanup once `parse_stack` is narrowed to the pure
// reducer.
